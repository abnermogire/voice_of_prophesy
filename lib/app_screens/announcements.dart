import 'package:flutter/material.dart';

class announcements extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Anouncements(UONSDA)"),
      ),
      body: Text("hey there"),
    );
  }
}