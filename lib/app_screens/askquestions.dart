import 'package:flutter/material.dart';

class askquestions extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Choose your area of query"),
      ),
      body: Text("hey there"),
    );
  }
}